# FastAPI Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy==2.0.23
alembic==1.12.1
# psycopg2-binary==2.9.9  # PostgreSQL adapter (commented out - using SQLite for now)

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Environment variables
python-dotenv==1.0.0

# Validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# CORS
# (included in FastAPI)

# Payment processing (when ready)
# stripe==7.4.0

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2  # For testing async endpoints
